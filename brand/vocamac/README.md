# VocaMac

Social card for vocamac.com. Same layout as the live OG. Proof line is now v0.9.0 beta.

- `vocamac-og.svg` / `vocamac-og.png`: 1200×630. Drop onto the site as `web/static/og-image.svg` and `web/static/og-image.png`.

The v0.8.0 card stays in `studies/2026-09-01-og-v0.8.0/`.
