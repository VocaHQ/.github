# VocaHQ illustrations

## Live

`voca-ecosystem-map.png` — hero ecosystem map for vocahq.com (`web/assets/illustrations/`). Compound product names (`VocaPhone`, `VocaLinux`, `VocaMac`, `VocaWin`, `VocaGateway`). Gateway caption: optional self-hosted compute on a host you run · never on-device. No LOCAL FIRST stamp (that stamp is HTML in How it works, not this PNG).

OG `web/assets/og/` embeds this same file.

## Studies

`studies/2026-09-08-ecosystem-map/` — pre-fix spaced-name capture and an SVG rebuild experiment.
